<h1> New Bookmark  </h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>
<a href="<%= lists_path%>" class= "btn btn-primary mt-4" style="text-decoration: none; "> Back </a>

