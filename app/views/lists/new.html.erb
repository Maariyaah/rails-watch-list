<h1> New List </h1>

<%= form_with model: @list do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<a href="<%=lists_path %>" class= "btn btn-primary" style= "text-decoration: none;"> Back to Lists </a>
